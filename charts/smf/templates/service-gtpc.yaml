apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-smf-gtpc
  labels: {{- include "open5gs.labels" . | nindent 4 }}
spec:
  ports:
  - name: gtpc
    port: {{ .Values.gtpc }}
    targetPort: gtpc
    protocol: UDP
  publishNotReadyAddresses: true
  selector: {{- include "open5gs.labels" . | nindent 4 }}
