name: compose
on:
  push:
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    #- uses: actions/checkout@v4

    - name: Set Up
      run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login ghcr.io -u ${{ vars.DOCKERHUB_USERNAME }} --password-stdin

