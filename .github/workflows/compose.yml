name: compose
on:
  push:
  #workflow_run:
  #  workflows:
  #  - images
  #  types:
  #  - completed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install
      run: sudo apt update && sudo apt search linux-modules

    - name: Prep
      run: sudo modprobe gtp

    - name: Set Up
      run: docker compose --profile test up --detach

    - name: pre check
      run: docker compose --profile test ps --all

    - name: logs
      run: docker compose --profile test logs test

    - name: Wait
      run: docker compose --profile test wait test

    - name: Check
      run: docker compose --profile test ps --all

    - name: Logs
      run: docker compose --profile test logs

    - name: Tear Down
      run: docker compose --profile test down --volumes
