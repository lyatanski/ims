name: charts
on:
  schedule:
  - cron: '0 1 * * SUN'
  push:
permissions: write-all

jobs:
  charts:
    runs-on: ubuntu-latest
    #strategy:
    #  matrix:
    #    include:
    #    - context: test
    #    - context: kamailio
    #    - context: rtpengine
    #    - context: freediameter
    #    - context: openssl
    #    - context: open5gs

    steps:
    - uses: actions/checkout@v4

    - name: Check
      run: helm version

